# 更新日志

## 未发布

### 修复

- **Debian 12 上建不了 HTTPS 站点**：站点配置固定渲染 `http2 on;`，这是 nginx 1.25.1 才有的指令，而 Debian 12 官方源是 1.22.1，`nginx -t` 直接报 unknown directive，导致所有 HTTPS 站点建不起来。现在按本机 nginx 版本自动选择 `http2 on;` 或 `listen ... http2`。
- **改动可能没生效却报告成功**：站点配置写盘与软链此前不检查返回值。磁盘满或目录只读时新配置根本没落盘，而 `nginx -t` 拿旧配置通过，于是提示「已更新」。现在每一步都检查，失败即回滚并明确报错。
- **端口封锁状态显示不实**：此前只要任意一条 iptables 链有规则就显示「已封锁」。Docker 重启会重建 `DOCKER-USER` 链并清掉规则，而 `INPUT` 链的还在，于是状态显示已封锁、端口实际可公网直连。现在要求所有链都有规则才算封锁。
- **端口封锁在重启后丢失**：Docker 或主机重启会清掉 iptables 规则。现在封锁的端口会被记录，脚本每次启动自动补回。
- **`gzip` 去重误伤局部配置**：此前会注释掉 `nginx.conf` 里所有 gzip 行，包括你写在 `server{}` / `location{}` 内的。现在只处理 `http{}` 直接子级。
- **非交互环境下菜单无限刷屏**：stdin 结束（EOF）时菜单把它当成「无效选项」反复重试。现在直接退出。
- **删除证书可能误删整个证书目录**：站点元信息损坏时主域名可能为空，`rm -rf` 的路径会退化成证书根目录。现在校验域名合法性后才执行。
- 缓存清理不再漏删/误删形近域名的条目。

### 安全

- 配置测试的错误输出改用 `mktemp`，不再写 `/tmp` 下的固定文件名（可预测路径可被本地用户预置软链，导致 root 覆盖任意文件）。
- 自定义登录 cookie、反代目标的主机与路径新增字符校验。这些值会被直接拼进 nginx 配置，此前缺少校验。
- 站点元信息在渲染前统一校验，覆盖手工编辑损坏与外部配置导入两种来源。
- 证书私钥目录与文件权限收紧的行为保持不变（`700` / `600`）。

### 新增

- **主菜单直接重载**：新增「6. 重载 Nginx 配置」，改完配置不必再进三层菜单。现有 1–5 / 9 / 0 编号不变。
- **操作记录**：每次改动写入 `/var/log/nginx-rp.log`，含时间与操作者。菜单「运维 → 查看本脚本操作记录」可查看。超 10MB 自动截断。
- **单实例保护**：第二个实例会被拒绝启动，避免两个窗口同时改配置互相覆盖。
- **主配置改动前自动备份**：修改 `/etc/nginx/nginx.conf` 前备份到 `/etc/nginx/nginx-rp-backups/`，并在 README 中完整披露改了哪三处、为什么。
- **卸载时可一并移除本脚本**：卸载 Nginx 后追问是否移除脚本本体与快捷命令 `n`。
- 外部配置导入时，会明确告知哪些 `allow` / `set_real_ip_from` 条目因模板无法表达而被丢弃。

### 性能

- 缓存清理从「全盘全文扫描」改为「只读文件头」。缓存区上限 10GB，此前删站或换缓存档会分钟级阻塞且没有任何提示。

### 工程

- 新增单测（`tests/unit.sh`，174 项，零依赖）与 Docker 集成测试（真实 Debian + nginx）。
- 新增 CI：`shellcheck -S warning` 零告警、语法检查、LF 行尾检查、单测、集成测试。
- 站点属性从 11 个位置参数改为具名字段的关联数组契约。落盘格式不变，**存量站点无需任何迁移**。
- 新增内部设计文档 `docs/ARCHITECTURE.md`。
